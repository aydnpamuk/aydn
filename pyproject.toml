[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aydn"
version = "0.1.0"
description = "Codex Python starter project"
authors = [{ name = "AYDIN" }]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
